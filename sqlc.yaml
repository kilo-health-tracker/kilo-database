version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries/"
    schema: "sql/ddl/"
    gen:
      go:
        package: "models"
        out: "models"
        emit_json_tags: true
        json_tags_case_style: "camel"